"use strict"

module.exports = {
    init: function (app) {
        // app.set('views', 'views')
        // app.set('view engine', 'jsx')
        // app.engine('jsx', require('express-react-views').createEngine())
    }
}